import { css } from 'lit';

/**
 * The background color of the main content area.
 */
export const mainBackgroundColor = css`var(--sl-color-neutral-50)`;

/**
 * The background color of the selected text.
 */
export const selectedTextBackgroundColor = css`var(--sl-color-neutral-900)`;
/**
 * The color of the selected text.
 *
 * Matches the main background color to provide an inverted color scheme.
 */
export const selectedTextColor = css`var(--sl-color-neutral-50)`;
